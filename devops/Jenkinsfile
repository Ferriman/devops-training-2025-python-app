pipeline {
  agent any
  stages {
    stage('Checkout') { steps { checkout scm } }
    stage('Build') { steps { echo 'Build placeholder' } }
    stage('Test') { steps { echo 'Test placeholder' } }
  }
}
